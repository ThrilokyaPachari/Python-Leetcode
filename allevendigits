def allevendigits(s,e)
    values=[]
    for i in range(s,e+1)
        s=str(i)
        if ((int(s[0]))%2==0 and (int(s[1])%2==0) and (int(s[2])%2==0) and (int(s[3])%2==0)):
            values.append(i)
    return values
print(allevendigits(1000,3000))
  
