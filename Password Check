Passwords=input("Enter the passwords').split(",")
valid=[]
for p in Passwords:
    if 6<=len(p)<=12 and any (c.isupper() for c in p) and any (c.islower() for cin p) and any(c.isdigit() for c in p) and any (c in "$#@" for c in p):
       valid.append(p)
print(valid)
