Define a function which can generate and print a tuple where the value are square of numbers between 1 and 20 (both included).

def squaretuple(n):
    values=[]
    for i in range(n+1):
        values.append(i**2)
    return(tuple(values))
print(squaretuple(20))
